[pytest]
testpaths = tests midi_workstation/tests
pythonpath = .
addopts = -q --tb=short
norecursedirs = PA800_Profile_Optimizer_SMART_MAX .venv midi_velocity_optimizer
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: longer integration or stress tests
